diff -urp ./views_content/plugins/views/views_content_plugin_display_ctools_context.inc ../ctools/views_content/plugins/views/views_content_plugin_display_ctools_context.inc
--- ./views_content/plugins/views/views_content_plugin_display_ctools_context.inc	2010-10-11 17:18:23.000000000 -0500
+++ ../ctools/views_content/plugins/views/views_content_plugin_display_ctools_context.inc	2011-01-31 17:08:44.000000000 -0600
@@ -33,6 +33,7 @@ class views_content_plugin_display_ctool
     $options['defaults']['default']['style_options'] = FALSE;
     $options['defaults']['default']['row_plugin'] = FALSE;
     $options['defaults']['default']['row_options'] = FALSE;
+    $options['inherit_panels_path'] = array('default' => 0);
 
     return $options;
   }
@@ -108,6 +109,12 @@ class views_content_plugin_display_ctool
       'value' => $admin_title,
     );
 
+    $options['inherit_panels_path'] = array(
+      'category' => 'context',
+      'title' => t('Use Panel path'),
+      'value' => $this->get_option('inherit_panels_path') ? t('Yes') : t('No'),
+    );
+
   }
 
   /**
@@ -130,6 +137,16 @@ class views_content_plugin_display_ctool
           '#description' => t('This is the title that will appear for this view context in the configure context dialog. If left blank, the view name will be used.'),
         );
         break;
+      case 'inherit_panels_path':
+        $form['#title'] .= t('Inherit path from panel display');
+
+        $form['inherit_panels_path'] = array(
+          '#type' => 'select',
+          '#options' => array(1 => t('Yes'), 0 => t('No')),
+          '#default_value' => $this->get_option('inherit_panels_path'),
+          '#description' => t('If yes, all links generated by Views, such as more links, summary links, and exposed input links will go to the panels display path, not the view, if the display has a path.'),
+        );
+        break;
     }
   }
 
@@ -142,15 +159,16 @@ class views_content_plugin_display_ctool
     parent::options_submit($form, $form_state);
     switch ($form_state['section']) {
       case 'admin_title':
+      case 'inherit_panels_path':
         $this->set_option($form_state['section'], $form_state['values'][$form_state['section']]);
         break;
     }
   }
 
-  /**
-   * Block views use exposed widgets only if AJAX is set.
-   */
-  function uses_exposed() {
-    return FALSE;
+  function get_path() {
+    if ($this->get_option('inherit_panels_path')) {
+      return $_GET['q'];
+    }
+    return parent::get_path();
   }
 }
